language: python

python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6

sudo: false

install:
  - pip install tox-travis
  - pip install "coverage<=4.0" "coveralls>=0.5" codecov

script:
  - tox -e $TOXENV

after_success:
  - coverage combine
  - coveralls
  - codecov
